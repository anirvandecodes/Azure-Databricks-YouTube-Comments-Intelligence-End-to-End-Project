resources:
  dashboards:
    youtube_intelligence_dashboard:
      display_name: "YouTube Intelligence Dashboard"
      file_path: ../dashboards/youtube_intelligence.lvdash.json
      warehouse_id: ${var.warehouse_id}
      parent_path: /Workspace/Users/${workspace.current_user.userName}
permissions:
        - level: CAN_VIEW
          group_name: users
